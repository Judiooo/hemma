services:
  portal:
    build: ./app
    container_name: hemma
    restart: unless-stopped
    ports:
      - "8000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
      - DB_PATH=/app/data/app.db
